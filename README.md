# pingpoint
pingpoint — a smart, cross-platform network diagnostic tool. Runs layered checks to pinpoint why you have no internet (DHCP, DNS, gateway, captive portal, ISP, MTU, Wi-Fi), explains it in plain language, and can auto-fix common causes. Pure Python 3, zero dependencies. Works on Linux, macOS &amp; Windows.
